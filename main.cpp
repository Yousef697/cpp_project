#include <iostream>
#include <cstring>
#include <string>
#include <string_view>
#include <sstream>

#include "Log.h"
#include "pointers.h"
#include "classes.h"

/// const int* => can change where the pointer points, but not the content
/// int* const => can change the content, but not where the pointer points
/// int const* => same as int* const
/// const int* const => cannot change where the pointer points or the content

int main()
{


    std::cin.get();

    return 0;
}
